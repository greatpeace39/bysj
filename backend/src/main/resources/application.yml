spring:
  application:
    name: fintech-backend
  datasource:
    url: jdbc:mysql://localhost:3306/fintech_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080

security:
  jwt:
    secret: ReplaceWithAtLeast32CharactersSecretKey12345
    expire-seconds: 86400
